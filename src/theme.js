/**
 * theme.js
 * Earth-tone minimalist palette.
 * - background : Beige
 * - text/border: Dark Brown
 * - primary    : Indigo (action buttons)
 *
 * Centralizing colors here keeps the UI consistent and easy to retune.
 */
export const COLORS = {
  beige: '#F3ECE0', // main background
  beigeSoft: '#FBF7F0', // cards / input fields
  darkBrown: '#3E2C23', // typography & item borders
  brownMuted: '#8C7A6B', // secondary text / placeholders
  indigo: '#3F51B5', // primary action buttons
  indigoPressed: '#32408F', // pressed state for buttons
  danger: '#A6402F', // destructive actions (delete)
  white: '#FFFFFF',
};

export const SPACING = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
};

export const RADIUS = {
  sm: 8,
  md: 12,
  lg: 16,
};
